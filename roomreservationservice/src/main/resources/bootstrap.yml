spring:
  application.name: roomreservationservice
  cloud:
    config:
      failFast: true
      retry:
        max-attempts: 60
      discovery:
        enabled: false
      uri: 'http://configserver:9000'
